# Formatter design

- [x] Define stack-column placement from token stack effects.
- [x] Define how a physical line folds several tokens into one visual unit.
- [x] Record the proposed style and its constraints in `IDEAS.md`.

Preserve the pre-existing uncommitted change in `stdlib/json.frog`.
