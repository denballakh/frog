# Snapshot: code/if_blocks

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 == if do 5 else 7 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: if or while requires a condition before do
[EXIT CODE] 1
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 == do 5 else 7 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
7
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 else 7 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
5
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 else 7 8 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: if branches leave different stack shapes
[EXIT CODE] 1
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 else 7 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: procedure output stack depth mismatch
[EXIT CODE] 1
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 else end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: if branches leave different stack shapes
[EXIT CODE] 1
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow stack shape mismatch
[EXIT CODE] 1
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 != do 5 print end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
5
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    5 if 1 2 == do drop 7 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
5
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    5 if 1 2 != do drop 7 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
7
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 == do
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 == do 5 print else
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 14

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 == else 5 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: else requires a preceding if arm and do
[EXIT CODE] 1
```

## Case 15

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if do end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: if or while requires a condition before do
[EXIT CODE] 1
```

## Case 16

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 do end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 17

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 do end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 18

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 3 do end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 19

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if 1 2 3 == do end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow stack shape mismatch
[EXIT CODE] 1
```

## Case 20

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    else
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow block stack underflow
[EXIT CODE] 1
```

## Case 21

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 if true do swap else end print print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
1
2
```

## Case 22

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if false do 1 elif true do 2 else 3 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
2
```

## Case 23

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if false do 1 elif false do 2 elif true do 3 else 4 end print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
3
```

## Case 24

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if false do 1 print elif true do 2 print end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
2
```

## Case 25

### Source: `test/tmp_fs/code.frog`

```frog
macro choose if false do 1 elif true do 2 else 3 end end

proc main -- do
    choose print
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
2
```

## Case 26

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    elif true do 1 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: elif outside active if
[EXIT CODE] 1
```

## Case 27

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if false do 1 else 2 elif true do 3 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: elif after else
[EXIT CODE] 1
```

## Case 28

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    if false do 1 elif do 2 end
end
```

### Run

Command:

```text
build/frogc run test/tmp_fs/code.frog
```

Output:

```text
frogc: elif requires a condition before do
[EXIT CODE] 1
```

