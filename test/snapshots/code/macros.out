# Snapshot: code/macros

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
macro dup let x do x x end end 1 dup ? print print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[INFO] test/tmp_fs/code.frog:1:38: Stack at compile time:
[NOTE] stack:
      0. INT()@test/tmp_fs/code.frog:1:32
      1. INT()@test/tmp_fs/code.frog:1:32
[INFO] test/tmp_fs/code.frog:1:38: Stack at runtime:
[NOTE] stack:
      0. INT():1@test/tmp_fs/code.frog:1:32
      1. INT():1@test/tmp_fs/code.frog:1:32
[PRINT] INT():1@test/tmp_fs/code.frog:1:32
[PRINT] INT():1@test/tmp_fs/code.frog:1:32
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
1
1

```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
macro swap let x y do y x end end 1 2 swap ? print print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[INFO] test/tmp_fs/code.frog:1:44: Stack at compile time:
[NOTE] stack:
      0. INT()@test/tmp_fs/code.frog:1:37
      1. INT()@test/tmp_fs/code.frog:1:35
[INFO] test/tmp_fs/code.frog:1:44: Stack at runtime:
[NOTE] stack:
      0. INT():2@test/tmp_fs/code.frog:1:37
      1. INT():1@test/tmp_fs/code.frog:1:35
[PRINT] INT():1@test/tmp_fs/code.frog:1:35
[PRINT] INT():2@test/tmp_fs/code.frog:1:37
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
1
2

```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
macro double dup + end 5 double print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[PRINT] INT():10@test/tmp_fs/code.frog:1:18
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
10

```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
macro inc 1 + end 5 inc print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[PRINT] INT():6@test/tmp_fs/code.frog:1:13
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
6

```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
5 later print macro later 1 + end
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[PRINT] INT():6@test/tmp_fs/code.frog:1:29
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
6

```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
macro choose if 1 2 == do 5 else 7 end end choose print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[PRINT] INT():7@test/tmp_fs/code.frog:1:34
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
7

```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
macro one 1 + end macro two one one end 5 two print
```

### Run

Command:

```text
py -m frog run test/tmp_fs/code.frog
```

Output:

```text
[PRINT] INT():7@test/tmp_fs/code.frog:1:13
```

### Build

Command:

```text
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[CMD] gcc test/tmp_fs/code.c -o test/tmp_fs/code.exe
[CMD] test/tmp_fs/code.exe
[STDOUT]:
7

```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
macro loop loop end loop
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:12: recursive macro expansion for loop
[NOTE] expansion_stack:
      0. Symbol(type=<SymbolType.MACRO: 'macro'>, module='', name='loop')
      1. Symbol(type=<SymbolType.MACRO: 'macro'>, module='', name='loop')
[EXIT CODE] 1
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
macro a b end macro b a end a
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:23: recursive macro expansion for a
[NOTE] expansion_stack:
      0. Symbol(type=<SymbolType.MACRO: 'macro'>, module='', name='a')
      1. Symbol(type=<SymbolType.MACRO: 'macro'>, module='', name='b')
      2. Symbol(type=<SymbolType.MACRO: 'macro'>, module='', name='a')
[EXIT CODE] 1
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
macro outer macro inner 1 end end
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:13: MACRO should not be inside of another MACRO
[EXIT CODE] 1
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
macro m else end
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:9: ELSE should follow an IF
[EXIT CODE] 1
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
macro
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:1: expected a name after MACRO
[EXIT CODE] 1
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
macro 123 end
```

### Run and Build

Commands:

```text
py -m frog run test/tmp_fs/code.frog
py -m frog -l WARN build -r test/tmp_fs/code.frog
```

Output:

```text
[ERROR] test/tmp_fs/code.frog:1:7: expected a name after MACRO, got TokenType.INT
[EXIT CODE] 1
```

