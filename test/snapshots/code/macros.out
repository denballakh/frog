# Snapshot: code/macros

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
macro dup let x do x x end end

proc main -- do
    1 dup ? print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
1
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
macro swap let x y do y x end end

proc main -- do
    1 2 swap ? print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
2
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
macro double dup + end

proc main -- do
    5 double print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
10
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
macro inc 1 + end

proc main -- do
    5 inc print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
6
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    5 later print
end

macro later 1 + end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
6
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
macro choose if 1 2 == do 5 else 7 end end

proc main -- do
    choose print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
7
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
macro one 1 + end
macro two one one end

proc main -- do
    5 two print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
7
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
macro loop loop end

proc main -- do
    loop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: recursive macro expansion
[EXIT CODE] 1
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
macro a b end
macro b a end

proc main -- do
    a
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: recursive macro expansion
[EXIT CODE] 1
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
macro outer macro inner 1 end end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: declarations are not allowed in macro bodies
[EXIT CODE] 1
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
macro m else end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: else outside macro if block
[EXIT CODE] 1
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
macro
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: expected macro name
[EXIT CODE] 1
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
macro 123 end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: expected word token
[EXIT CODE] 1
```

