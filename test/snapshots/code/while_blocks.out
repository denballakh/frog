# Snapshot: code/while_blocks

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    10 while dup 5 > do 1 - print ? end drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow stack shape mismatch
[EXIT CODE] 1
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    10 while dup 5 > do 1 - dup print ? end drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
9
8
7
6
5
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    0 while dup 3 < if dup do drop true end do 1 + end print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
3
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while else
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: while requires do before end
[EXIT CODE] 1
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while 1 2 == do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated procedure body
[EXIT CODE] 1
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while do end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: if or while requires a condition before do
[EXIT CODE] 1
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while 1 do end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while 1 2 do end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while 1 2 3 do end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    while 1 2 3 == do end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow stack shape mismatch
[EXIT CODE] 1
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: control-flow block stack underflow
[EXIT CODE] 1
```

## Case 14

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: only declarations and imports are allowed at top level
[EXIT CODE] 1
```

