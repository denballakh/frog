# Snapshot: code/literals_and_comments

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    '
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated character literal
[EXIT CODE] 1
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    ''
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: invalid character literal
[EXIT CODE] 1
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    'aa'
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: invalid character literal
[EXIT CODE] 1
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    '\n'
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: invalid character literal
[EXIT CODE] 1
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    '\t'
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: invalid character literal
[EXIT CODE] 1
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    '\''
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated character literal
[EXIT CODE] 1
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unterminated string literal
[EXIT CODE] 1
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "" ? drop drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "abc" ? drop drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "abc\n" ? drop drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "abc'" ? drop drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "abc\"" ? drop drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 14

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    "A\n\x42\0é" let p n do n print p @u8 print p 1 + @u8 print p 2 + @u8 print p 3 + @u8 print p 4 + @u8 print end
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
6
65
10
66
0
195
```

## Case 15

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 // comment
     print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
```

