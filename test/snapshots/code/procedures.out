# Snapshot: code/procedures

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
proc a int -- int do 2 * end

proc main -- do
    5 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
10
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
proc a do 2 * end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unknown type in procedure signature
[EXIT CODE] 1
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
proc a int do 2 * end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unknown type in procedure signature
[EXIT CODE] 1
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
proc a -- do 2 * end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc a int -- do 2 * end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: procedure output stack depth mismatch
[EXIT CODE] 1
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
proc a int -- int int do 2 * end

proc main -- do
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: procedure output stack depth mismatch
[EXIT CODE] 1
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
proc a int -- int do drop 5 end

proc main -- do
    5 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
5
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
proc a bool -- int do drop 5 end

proc main -- do
    5 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack type mismatch
[EXIT CODE] 1
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
proc a int int -- int do + end

proc main -- do
    5 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
proc a x -- y do + end

proc main -- do
    5 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: unknown type in procedure signature
[EXIT CODE] 1
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
proc a int int -- int do + end

proc main -- do
    5 7 a print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
12
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
proc ++ int -- int do 1 + end

proc main -- do
    5 ++ print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
6
```

