# Snapshot: code/stack_intrinsics

## Case 01

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 ? print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
2
1
```

## Case 02

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 dup ? print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
1
```

## Case 03

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 dup2 ? print print print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
2
1
2
1
```

## Case 04

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 drop ? print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
```

## Case 05

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 swap ? print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
2
```

## Case 06

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 3 ? print print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
3
2
1
```

## Case 07

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 3 rot ? print print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
1
3
2
```

## Case 08

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    1 2 3 4 ? swap2 ? print print print print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
2
1
4
3
```

## Case 09

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    print
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 10

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    ?
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
(no output)
```

## Case 11

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    dup
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 12

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    swap
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 13

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    drop
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

## Case 14

### Source: `test/tmp_fs/code.frog`

```frog
proc main -- do
    rot
end
```

### Run

Command:

```text
python -m frog run test/tmp_fs/code.frog
```

Output:

```text
frogc: compile-time stack underflow
[EXIT CODE] 1
```

