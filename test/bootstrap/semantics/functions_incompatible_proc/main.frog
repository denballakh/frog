fn Mapper int -- int end

record Slot
    value Mapper
end

proc incompatible bool -- int do drop 0 end
proc inc int -- int do 1 + end

proc main -- do
    Slot:alloc
    let slot do
        0 slot ptr cast !i64
        1 slot @Slot.value Mapper:call drop
    end
end
