from "stdlib/libc.frog" import alloc

union Maybe
    case none
    case some int
end

proc main -- do
    16 alloc
    let raw do
        2 i64 cast raw i64* cast !
        raw Maybe cast Maybe.none
    end
end
